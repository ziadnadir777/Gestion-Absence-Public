pipeline {
  agent any

  stages {
    stage('Test Connection') {
      steps {
        echo '✅ Jenkins is connected and running this pipeline!'
        sh 'echo Hello from Jenkins inside your repo!'
      }
    }
  }
}
